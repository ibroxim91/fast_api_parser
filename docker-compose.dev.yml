version: "3.9"
services:
  scraper:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8089:8089"
    networks:
      - backend_simple_travel

networks:
  backend_simple_travel:
    external: true
